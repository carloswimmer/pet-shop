# Improvements

## Todo list:

- [ ] create type for `period` in `period-section.tsx`
- [ ] refactor `period` inside of `Appointment` in `appointment.ts` (if `period` property is not used, lets use `reduce` 
instead of `map` + `filter` in `groupAppointmentsByPeriod()`)
- [ ] apply Figma's design in `appointment-card.tsx`